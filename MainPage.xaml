<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SimpleElevenlabsAndroid.MainPage"
             Loaded="OnLoaded">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label
                Text="Simple Elevenlabs"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center" />

            <Entry x:Name="MessageBox" Placeholder="Enter Text" Completed="OnEntryCompleted"/>
            <Picker x:Name="VoiceSelection" />

            <Button
                x:Name="CounterBtn"
                Text="Send"
                SemanticProperties.Hint="Sends your text to be generated"
                Clicked="OnCounterClicked"
                HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
